apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "booking-service-grpc.fullname" . }}-test-connection"
  labels:
    {{- include "booking-service-grpc.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "booking-service-grpc.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
